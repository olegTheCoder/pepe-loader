export interface PepeLoaderProps {
  size?: number;
  speed?: number;
  texts?: [string, string, string];
  colors?: [string, string, string];
  textColor?: string;
  fontSizes?: [number, number, number];
  fontWeight?: number | string;
  fontFamily?: string;
  burstStrokeWidth?: number;
}
